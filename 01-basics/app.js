/*
    Rules:
    ------
    1. must use camal case
    2. never starts with a number
    3. a-z ,A-Z, 0-9, _ , $
    4. max 15 characters
    5. case sensitive
    6. for const values 'const': ALL_CAPS
    7. Interfaces/ Classes, Enum can starts with Uppercase / Camelcase
    */
var courseName = 'Angular';
var course_1 = '';
var numberOne = 10;
var numberTwo = 20;
//version, SERVER URL, JWT_SECRET_KEY
var VERSION = '10.0.2';
console.log(VERSION);
